% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_POI.R
\name{add_POI}
\alias{add_POI}
\title{Add a point of interest (POI) with a labeled marker on a plot}
\usage{
add_POI(
  lat,
  lon,
  pch = 25,
  lineColor = "white",
  fillColor = "blue",
  textbg = rgb(1, 1, 1, 0.5),
  title = "",
  cex = 1.5
)
}
\arguments{
\item{lat, lon}{Numeric. Latitude (y-coordinate) and longitude (x-coordinate)
of the POI.}

\item{pch}{Point type of POI}

\item{lineColor, fillColor}{Character string or \code{\link[=rgb]{rgb()}} for the frame and fill
color of the POI, respectively}

\item{textbg}{Background color for text, if a title is provided}

\item{title}{Character. Text label to display next to the POI marker.
Default is an empty string.}

\item{cex}{A factor of the point size}
}
\value{
Invisibly returns NULL. The function is called for its side effects (adding graphics to an existing plot).
}
\description{
This function plots a "Point of Interest" (POI) at the specified latitude and longitude on the current plot.
It adds a colored triangular marker and an optional text label. If the label extends beyond the plot boundary,
its position is automatically adjusted to remain visible. The function is designed for use in spatial or map-based plotting contexts.
}
\details{
The function calculates text dimensions to draw a rectangular background behind the label and places
the text and marker such that they fit within the plot region.
The marker is drawn using pch = 25 (downward-pointing triangle).
}
\seealso{
\code{\link[=points]{points()}}, \code{\link[=text]{text()}}, \code{\link[=rect]{rect()}}
}
